{
  "name": "ai-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable server-side Anthropic API proxy on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
